<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Student Registration | DocSec</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #020617;
      --card: rgba(15, 23, 42, 0.96);
      --border: rgba(148, 163, 184, 0.55);
      --accent: #22c55e;
      --accent-soft: rgba(16, 185, 129, 0.16);
      --text: #e5e7eb;
      --muted: #9ca3af;
      --radius-xl: 20px;
      --shadow: 0 18px 55px rgba(15, 23, 42, 0.98);
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
      min-height: 100vh;
      background:
        radial-gradient(circle at top, rgba(16, 185, 129, 0.16), transparent 55%),
        radial-gradient(circle at bottom, rgba(37, 99, 235, 0.2), transparent 55%),
        #020617;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      color: var(--text);
    }
    .card {
      width: 100%;
      max-width: 520px;
      border-radius: var(--radius-xl);
      border: 1px solid var(--border);
      background: linear-gradient(140deg, rgba(15, 23, 42, 0.97), rgba(15, 23, 42, 0.9));
      box-shadow: var(--shadow);
      padding: 18px 18px 14px;
      position: relative;
      overflow: hidden;
    }
    .card::before {
      content: "";
      position: absolute;
      inset: -40%;
      background:
        radial-gradient(circle at 0 0, rgba(16, 185, 129, 0.2), transparent 60%),
        radial-gradient(circle at 100% 100%, rgba(59, 130, 246, 0.25), transparent 60%);
      opacity: 0.9;
      pointer-events: none;
      filter: blur(20px);
    }
    .card-inner {
      position: relative;
      z-index: 1;
    }
    .header-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 10px;
      margin-bottom: 10px;
    }
    h1 {
      font-size: 20px;
      letter-spacing: 0.03em;
    }
    .subtitle {
      font-size: 11px;
      color: var(--muted);
    }
    .badge {
      border-radius: 999px;
      border: 1px solid rgba(16, 185, 129, 0.7);
      padding: 3px 9px;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      background: rgba(15, 23, 42, 0.9);
    }
    form {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
      margin-top: 6px;
    }
    .full {
      grid-column: 1 / -1;
    }
    label {
      display: block;
      font-size: 11px;
      color: var(--muted);
      margin-bottom: 3px;
    }
    .input {
      width: 100%;
      padding: 8px 10px;
      border-radius: 12px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      background: rgba(15, 23, 42, 0.9);
      color: var(--text);
      outline: none;
      font-size: 12px;
      transition: border 0.18s ease-out, box-shadow 0.18s ease-out, background 0.18s ease-out;
    }
    .input:focus {
      border-color: rgba(16, 185, 129, 0.9);
      box-shadow: 0 0 0 1px rgba(16, 185, 129, 0.7);
      background: rgba(15, 23, 42, 0.98);
    }
    .helper {
      font-size: 10px;
      color: var(--muted);
      margin-top: 3px;
    }
    .primary-btn {
      grid-column: 1 / -1;
      border-radius: 999px;
      padding: 9px 10px;
      border: 1px solid rgba(16, 185, 129, 0.9);
      background: linear-gradient(135deg, #22c55e, #0ea5e9);
      color: #e5e7eb;
      font-size: 13px;
      font-weight: 500;
      cursor: pointer;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      box-shadow: 0 18px 40px rgba(16, 185, 129, 0.8);
      transition: transform 0.18s ease-out, box-shadow 0.18s ease-out;
      margin-top: 2px;
    }
    .primary-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 22px 50px rgba(16, 185, 129, 1);
    }
    .bottom-text {
      margin-top: 8px;
      font-size: 11px;
      color: var(--muted);
      text-align: center;
    }
    .bottom-text a {
      color: #4ade80;
      text-decoration: none;
    }
    .bottom-text a:hover {
      text-decoration: underline;
    }
    .tags {
      margin-top: 6px;
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      justify-content: center;
    }
    .tag {
      font-size: 10px;
      padding: 3px 9px;
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      background: rgba(15, 23, 42, 0.96);
      color: var(--muted);
    }
    @media (max-width: 640px) {
      form { grid-template-columns: minmax(0, 1fr); }
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="card-inner">
      <div class="header-row">
        <div>
          <h1>Create Student Account</h1>
          <p class="subtitle">Register once to access all PDFs shared with your class.</p>
        </div>
        <span class="badge">New</span>
      </div>

      <form action="/stu/regi" method="POST">
        <div>
          <label for="name">Full Name</label>
          <input
            id="name"
            type="text"
            name="name"
            class="input"
            placeholder="e.g., Arjun Mehta"
            required
          />
        </div>

        <div>
          <label for="enroll">Enrollment Number</label>
          <input
            id="enroll"
            type="text"
            name="enroll"
            class="input"
            placeholder="e.g., 23BCS001"
            required
          />
        </div>

        <div>
          <label for="email">College Email</label>
          <input
            id="email"
            type="email"
            name="email"
            class="input"
            placeholder="e.g., arjun.mehta@college.edu"
            required
          />
        </div>

        <div>
          <label for="contact">Contact Number</label>
          <input
            id="contact"
            type="text"
            name="contact"
            class="input"
            placeholder="10-digit mobile number"
            required
          />
        </div>

        <div class="full">
          <label for="password">Create Password</label>
          <input
            id="password"
            type="password"
            name="pass"
            class="input"
            placeholder="At least 6 characters"
            required
          />
          <p class="helper">
            Use something you’ll remember, but don’t reuse your social media password.
          </p>
        </div>

        <button type="submit" class="primary-btn">
          Complete registration
          <span>↗</span>
        </button>
      </form>

      <p class="bottom-text">
        Already have an account?
        <a href="/stu/login">Login here</a>
      </p>

      <div class="tags">
        <span class="tag">One login for all PDFs</span>
        <span class="tag">Works on mobile & laptop</span>
      </div>
    </div>
  </div>
</body>
</html>
