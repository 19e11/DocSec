<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Student Dashboard | DocSec</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #020617;
      --card: rgba(15, 23, 42, 0.95);
      --border: rgba(148, 163, 184, 0.5);
      --accent: #38bdf8;
      --accent-soft: rgba(56, 189, 248, 0.16);
      --text: #e5e7eb;
      --muted: #9ca3af;
      --danger: #fb7185;
      --radius-xl: 20px;
      --radius-lg: 14px;
      --shadow: 0 20px 60px rgba(15, 23, 42, 0.98);
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
      min-height: 100vh;
      background:
        radial-gradient(circle at top, rgba(56, 189, 248, 0.18), transparent 55%),
        radial-gradient(circle at bottom, rgba(129, 140, 248, 0.2), transparent 55%),
        #020617;
      color: var(--text);
      display: flex;
      align-items: stretch;
      justify-content: center;
      padding: 16px;
    }
    .layout {
      width: 100%;
      max-width: 1180px;
      display: grid;
      grid-template-columns: 230px minmax(0, 1fr);
      gap: 14px;
    }
    .sidebar {
      border-radius: var(--radius-xl);
      border: 1px solid var(--border);
      background: linear-gradient(150deg, rgba(15, 23, 42, 0.96), rgba(15, 23, 42, 0.9));
      padding: 16px 14px;
      box-shadow: var(--shadow);
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    .brand {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .brand-icon {
      width: 34px;
      height: 34px;
      border-radius: 12px;
      background: radial-gradient(circle at 20% 10%, #38bdf8, #6366f1);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      font-weight: 600;
      box-shadow: 0 16px 40px rgba(37, 99, 235, 0.9);
    }
    .brand-text span:first-child {
      font-size: 15px;
      font-weight: 600;
    }
    .brand-text span:last-child {
      font-size: 11px;
      color: var(--muted);
    }
    .nav-title {
      font-size: 11px;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      color: var(--muted);
      margin-bottom: 4px;
      margin-top: 4px;
    }
    .nav {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }
    .nav li a {
      display: flex;
      justify-content: space-between;
      align-items: center;
      text-decoration: none;
      font-size: 12px;
      color: var(--muted);
      border-radius: var(--radius-lg);
      padding: 7px 9px;
      border: 1px solid transparent;
      transition: background 0.18s ease-out, border 0.18s ease-out, color 0.18s ease-out;
    }
    .nav li a.active,
    .nav li a:hover {
      background: radial-gradient(circle at top left, rgba(56, 189, 248, 0.18), transparent 45%),
                  rgba(15, 23, 42, 0.98);
      border-color: rgba(56, 189, 248, 0.8);
      color: var(--text);
    }
    .nav-badge {
      font-size: 10px;
      padding: 2px 7px;
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      background: rgba(15, 23, 42, 0.95);
    }
    .sidebar-footer {
      margin-top: auto;
      font-size: 11px;
      color: var(--muted);
    }
    .logout-btn {
      margin-top: 8px;
      width: 100%;
      border-radius: 999px;
      padding: 7px 8px;
      border: 1px solid rgba(248, 113, 113, 0.8);
      background: rgba(127, 29, 29, 0.7);
      color: #fee2e2;
      font-size: 12px;
      cursor: pointer;
      transition: transform 0.18s ease-out, box-shadow 0.18s ease-out;
      box-shadow: 0 14px 32px rgba(127, 29, 29, 0.9);
    }
    .logout-btn:hover {
      transform: translateY(-1px);
      box-shadow: 0 20px 40px rgba(127, 29, 29, 1);
    }

    .main {
      border-radius: var(--radius-xl);
      border: 1px solid var(--border);
      background: radial-gradient(circle at top, rgba(15, 23, 42, 0.98), rgba(15, 23, 42, 0.94));
      box-shadow: var(--shadow);
      padding: 16px 16px 14px;
      display: flex;
      flex-direction: column;
      gap: 14px;
    }
    .header-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 10px;
      flex-wrap: wrap;
    }
    .header-row h1 {
      font-size: 20px;
    }
    .header-row p {
      font-size: 11px;
      color: var(--muted);
    }
    .badge-pill {
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.14em;
      padding: 2px 8px;
      border-radius: 999px;
      border: 1px solid rgba(56, 189, 248, 0.8);
      background: rgba(15, 23, 42, 0.95);
      color: var(--accent);
    }

    .top-bar {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      align-items: center;
      margin-top: 4px;
    }
    .search {
      flex: 1;
      min-width: 180px;
      display: flex;
      align-items: center;
      gap: 6px;
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      background: rgba(15, 23, 42, 0.9);
      padding: 6px 9px;
      font-size: 12px;
      color: var(--muted);
    }
    .search input {
      border: none;
      outline: none;
      background: transparent;
      color: var(--text);
      flex: 1;
      font-size: 12px;
    }
    .chip {
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.6);
      padding: 5px 9px;
      font-size: 11px;
      color: var(--muted);
      background: rgba(15, 23, 42, 0.9);
    }

    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 10px;
      margin-top: 4px;
    }
    .pdf-card {
      border-radius: var(--radius-lg);
      border: 1px solid rgba(31, 41, 55, 0.9);
      background: radial-gradient(circle at top left, rgba(56, 189, 248, 0.12), transparent 55%),
                  rgba(15, 23, 42, 0.98);
      padding: 10px 10px 8px;
      display: flex;
      flex-direction: column;
      gap: 6px;
      position: relative;
      overflow: hidden;
      transition: transform 0.18s ease-out, box-shadow 0.18s ease-out, border 0.18s ease-out;
    }
    .pdf-card::before {
      content: "";
      position: absolute;
      inset: -40%;
      background: radial-gradient(circle at 100% 0, rgba(56, 189, 248, 0.18), transparent 55%);
      opacity: 0.8;
      pointer-events: none;
      filter: blur(16px);
    }
    .pdf-inner {
      position: relative;
      z-index: 1;
    }
    .pdf-card:hover {
      transform: translateY(-2px);
      border-color: rgba(56, 189, 248, 0.85);
      box-shadow: 0 18px 40px rgba(15, 23, 42, 0.95);
    }
    .pdf-title {
      font-size: 14px;
      font-weight: 500;
    }
    .pdf-meta {
      font-size: 11px;
      color: var(--muted);
    }
    .pdf-teacher {
      font-size: 11px;
      color: var(--muted);
    }
    .status-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 2px;
      font-size: 10px;
      color: var(--muted);
    }
    .status-pill {
      border-radius: 999px;
      border: 1px solid rgba(56, 189, 248, 0.7);
      background: rgba(15, 23, 42, 0.98);
      padding: 3px 8px;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 10px;
    }
    .status-dot {
      width: 7px;
      height: 7px;
      border-radius: 999px;
      background: #22c55e;
      box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.3);
    }
    .btn-row {
      display: flex;
      justify-content: flex-end;
      gap: 6px;
      margin-top: 6px;
      font-size: 11px;
    }
    .btn-secondary,
    .btn-primary {
      border-radius: 999px;
      padding: 5px 9px;
      border: 1px solid rgba(148, 163, 184, 0.7);
      background: rgba(15, 23, 42, 0.98);
      color: var(--muted);
      cursor: pointer;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: transform 0.18s ease-out, box-shadow 0.18s ease-out, border 0.18s ease-out, background 0.18s ease-out;
    }
    .btn-primary {
      border-color: rgba(56, 189, 248, 0.9);
      background: linear-gradient(135deg, #0ea5e9, #6366f1);
      color: #e5e7eb;
      box-shadow: 0 14px 30px rgba(37, 99, 235, 0.9);
    }
    .btn-secondary:hover,
    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 16px 36px rgba(15, 23, 42, 0.9);
    }

    .empty-state {
      padding: 40px 16px;
      text-align: center;
      font-size: 12px;
      color: var(--muted);
      border-radius: var(--radius-lg);
      border: 1px dashed rgba(51, 65, 85, 0.9);
      background: rgba(15, 23, 42, 0.9);
      margin-top: 8px;
    }

    @media (max-width: 900px) {
      .layout { grid-template-columns: minmax(0, 1fr); }
      .sidebar { flex-direction: row; align-items: center; }
      .nav-title, .nav, .sidebar-footer { display: none; }
    }

    @media (max-width: 640px) {
      body { padding: 10px; }
    }
  </style>
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <div class="brand">
        <div class="brand-icon">S</div>
        <div class="brand-text">
          <span><%= user.name %></span>
          <span>Student • Signed in</span>
        </div>
      </div>

      <div>
        <p class="nav-title">Navigation</p>
        <ul class="nav">
          <li>
            <a href="#" class="active">
              <span>All PDFs</span>
              <span class="nav-badge">Default</span>
            </a>
          </li>
          <li>
            <a href="#">
              <span>Exam / Quiz PDFs</span>
              <span class="nav-badge">Soon</span>
            </a>
          </li>
          <li>
            <a href="#">
              <span>Important only</span>
              <span class="nav-badge">Soon</span>
            </a>
          </li>
        </ul>
      </div>

      <div class="sidebar-footer">
        <div>Tip: Open exam PDFs first – your teacher sees this.</div>
        <form action="/logout" method="GET">
          <button class="logout-btn" type="submit">Logout</button>
        </form>
      </div>
    </aside>

    <main class="main">
      <div class="header-row">
        <div>
          <h1>Available PDFs</h1>
          <p>View and download all PDFs shared with your classes in one place.</p>
        </div>
        <span class="badge-pill">Dashboard</span>
      </div>

      <div class="top-bar">
        <div class="search">
          <span>🔍</span>
          <input id="searchInput" type="text" placeholder="Search by title or teacher name..." />
        </div>
        <span class="chip">Today: focus on exam-related PDFs first.</span>
      </div>

      <% if (posts.length === 0) { %>
        <div class="empty-state">
          No PDFs uploaded yet. Once your teacher uploads notes or exam files, they
          will appear here automatically.
        </div>
      <% } else { %>
        <div class="cards" id="pdfList">
          <% posts.forEach(post => { %>
            <article class="pdf-card" data-title="<%= (post.title || '') + ' ' + (post.description || '') + ' ' + (post.uploadedBy ? post.uploadedBy.fullname : '') %>">
              <div class="pdf-inner">
                <h2 class="pdf-title"><%= post.title %></h2>
                <% if (post.description) { %>
                  <p class="pdf-meta"><%= post.description %></p>
                <% } else { %>
                  <p class="pdf-meta">No description provided. Ask your teacher what this is about.</p>
                <% } %>

                <% if (post.uploadedBy) { %>
                  <p class="pdf-teacher">
                    Uploaded by
                    <strong><%= post.uploadedBy.fullname %></strong>
                  </p>
                <% } %>

                <div class="status-row">
                  <span>
                    Status:
                    <span class="status-pill">
                      <span class="status-dot"></span>
                      Ready to open
                    </span>
                  </span>
                  <span>Click “Open PDF” to view.</span>
                </div>

                <div class="btn-row">
                  <button type="button" class="btn-secondary" onclick="copyLink('<%= '/dl/' + post._id %>')">
                    Copy link
                  </button>
                  <a class="btn-primary" href="/dl/<%= post._id %>">
                    Open PDF
                    <span>↗</span>
                  </a>
                </div>
              </div>
            </article>
          <% }) %>
        </div>
      <% } %>
    </main>
  </div>

  <script>
    // Simple client-side search/filter
    const searchInput = document.getElementById('searchInput');
    const pdfList = document.getElementById('pdfList');
    if (searchInput && pdfList) {
      searchInput.addEventListener('input', () => {
        const query = searchInput.value.toLowerCase();
        const items = pdfList.querySelectorAll('.pdf-card');
        items.forEach(card => {
          const text = card.dataset.title.toLowerCase();
          card.style.display = text.includes(query) ? '' : 'none';
        });
      });
    }

    // Copy PDF link helper
    function copyLink(url) {
      const full = window.location.origin + url;
      navigator.clipboard.writeText(full)
        .then(() => {
          alert('Link copied! You can paste it into WhatsApp or anywhere.');
        })
        .catch(() => {
          alert('Unable to copy link. You can manually copy from the address bar.');
        });
    }
  </script>
</body>
</html>
